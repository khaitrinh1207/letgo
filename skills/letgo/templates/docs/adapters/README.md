# Document adapters

Adapters are project-owned readers for external sources such as SharePoint, local hard copies, or
URLs. They return a resolved revision and local readable path; they never edit the source system.

